
TCPLIB(3)		C LIBRARY FUNCTIONS			TCPLIB(3)

名前
	tcplib		TCP/IP 通信処理ライブラリ

形式
	#include	"tcplib.h"

機能説明
	tcplib は TCP/IP 通信処理を行なうライブラリです。単体では意味をな
	しません。

バグ
	特権ポートを用いた通信への対応は不完全です。
	信頼性は保証できませんが、このライブラリを使用したプログラム
	で現在のところ特に支障は起きていません。
